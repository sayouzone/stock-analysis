{
  "name": "stocks-analysis-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for stocks analysis backend and frontend",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start-backend": "concurrently \"uvicorn main:app --reload\" \"npm run dev --prefix frontend\"",
    "start-frontend": "npm run dev --prefix frontend",
    "start-all": "npm run build --prefix frontend && concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-uvicorn-only": "npm run build --prefix frontend && uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
